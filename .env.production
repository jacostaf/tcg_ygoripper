# Production Environment Configuration
# This file shows how to configure the application for production use
# Copy this to .env and modify values as needed

# Allow the application to start even if database connection fails
# This prevents 500 errors when MongoDB is temporarily unavailable
ALLOW_START_WITHOUT_DATABASE=1

# Memory limit in MB (adjust based on your server capacity)
MEM_LIMIT=512

# Log level for production (INFO recommended)
LOG_LEVEL=INFO

# Debug mode (should be False in production)
DEBUG=False

# Port for the application
PORT=8081

# MongoDB connection string (replace with your actual connection string)
# MONGODB_CONNECTION_STRING=mongodb+srv://username:password@cluster.mongodb.net/database

# TCGPlayer optimization settings (optional, defaults are usually fine)
# TCGPLAYER_MAX_PREFERRED_RESULTS=50
# TCGPLAYER_MAX_ACCEPTABLE_RESULTS=200
# TCGPLAYER_DEFAULT_VARIANT_LIMIT=100